# Copy this file to .env and fill in real values.
# .env is gitignored — never commit secrets.

# CEP lookup base URL (public API, no key required)
CEP_API_URL=https://viacep.com.br/ws

# Example for future APIs that require authentication:
# SOME_API_KEY=
# SOME_API_SECRET=
